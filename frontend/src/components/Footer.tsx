import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>© {new Date().getFullYear()} פודיז בע"מ — כל הזכויות שמורות.</p>
        </footer>
    );
};

export default Footer;